<Window x:Class="WorkerHaCS.Views.HouseInformationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkerHaCS.Views"
        mc:Ignorable="d"
        Title="" Height="200" Width="310"
        ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label>Улица</Label>
                <ComboBox x:Name="StreetComboBox" DisplayMemberPath="Name" Width="180" Margin="62,0,0,0">
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0, 10, 0, 0">
                <Label>Номер дома</Label>
                <TextBox x:Name="HouseNumberTextBox"  Style="{StaticResource TextBox}" Margin="27, 0, -207, 0" Width="180"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0, 10, 0, 0">
                <Label>Кол-во квартир</Label>
                <TextBox x:Name="NumberOfApartmentsTextBox"  Style="{StaticResource TextBox}" Margin="10, 0, -190, 0"/>
            </StackPanel>
            <Button x:Name="SaveButton" Style="{StaticResource Button}" Content="Сохранить"  Margin="0,10,0,0" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
